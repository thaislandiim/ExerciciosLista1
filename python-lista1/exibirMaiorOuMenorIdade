atual = 2022

nome = input("Digite seu nome: ")
anoInserido = input("Digite o ano que você nasceu: ")

idade = atual - int(anoInserido)

if idade >= 18:
  print(nome + " você é maior de idade")
else:
  print(nome + " você é menor de idade")